<h1><%= @article.title %></h1>
<p><%= @article.content %></p>
<%= link_to "Edit", edit_article_path(@article.id) %>
<%= link_to "Destroy", article_path(@article.id), method: :delete, data: {confirm: "Are you sure you want to delete #{@article.title}?"}   %>
